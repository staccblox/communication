package Sample api;

fn Main() -> i32 {
    var a: auto = "sharing information within and outside of an organization.";
    var b: auto = "Use complex typography for graphical substitutes to verbal mediums.";
    Print(a);
    Print(b);
    return 0;
}
